# =============================================================================
# EdgeSim .gitignore —— 版本控制忽略规则
# =============================================================================
# 【原则】
#   1. 忽略构建产物（build 目录）
#   2. 忽略运行时生成的数据（数据库、报表、录音、截图）
#   3. 忽略大体积二进制（模型文件、生成的中文字体）
#   4. 忽略 IDE / 系统临时文件
# =============================================================================

# -----------------------------------------------------------------------------
# 一、构建目录（x86 桌面与 ARM64 交叉编译）
# -----------------------------------------------------------------------------
build/
build-*/
build_*/
cmake-build-*/

# -----------------------------------------------------------------------------
# 二、运行时生成数据（不提交，避免仓库膨胀与隐私）
# -----------------------------------------------------------------------------
data/                  # SQLite 数据库（edgesim.db）运行目录
*.db
*.db-journal
perf_report.txt        # 性能报表（退出时生成）
*.wav                  # 录音临时文件
screenshot*.bmp        # 截图

# -----------------------------------------------------------------------------
# 三、大体积二进制与生成文件
# -----------------------------------------------------------------------------
models/llm/*.gguf                # AI 模型文件（GGUF/ONNX/bin，需单独下载）
ui_lvgl/lv_font_chinese_16.c   # 中文字体（lv_font_conv 生成，约 3MB）
ui_lvgl/lv_font_chinese_16.c.bak

# -----------------------------------------------------------------------------
# 四、IDE / 编辑器 / 系统文件
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# 五、测试临时文件
# -----------------------------------------------------------------------------
/tmp/rag_test_kb.txt   # 测试产生的临时知识库（在项目目录下运行时）
models/
*.rknn
*.onnx
*.tar
build/
models/
build/
*.onnx
*.rknn
